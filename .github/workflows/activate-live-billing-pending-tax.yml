name: Legacy Pending-Tax Billing Activation (Deprecated)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  blocked:
    name: Use audited payment readiness controls
    runs-on: ubuntu-latest
    steps:
      - name: Block deprecated activation path
        shell: bash
        run: |
          echo "::error::This legacy workflow directly wrote payment readiness and is retired. Use docs/P2_DISPATCH_PRODUCTION_CUTOVER.md and the audited setPaymentProviderReadiness control after provider acceptance."
          exit 1
