{
  "name": "pipe-buyer-admin-agent",
  "description": "Fail-closed administrative agent entry point for Pipe Buyer",
  "private": true,
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "scripts": {
    "check": "node --check index.js && node --check agent_policy.js && npm test",
    "lint": "eslint --max-warnings=0 .",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.3.2"
  },
  "overrides": {
    "brace-expansion": "5.0.9",
    "uuid": "11.1.1"
  },
  "devDependencies": {
    "eslint": "10.7.0",
    "globals": "17.7.0"
  }
}
