# The administrative agent is unavailable unless this is explicitly enabled.
PIPE_AGENT_ENABLED=false

# Disabled deployments should use zero warm instances. Increase only with an
# approved Phase 2 release and matching GCP budget alerts.
PIPE_AGENT_MIN_INSTANCES=0
PIPE_AGENT_MAX_INSTANCES=3
